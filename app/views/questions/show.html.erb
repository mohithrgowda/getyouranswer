<!-- <p style="color: green"><%= notice %></p> -->

<%= render @question %>

<div>
  <%= link_to "Edit this question", edit_question_path(@question) %> |<%= link_to "Destroy", question_path(@question), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
</div>
