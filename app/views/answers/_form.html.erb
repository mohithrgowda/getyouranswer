<%= form_with(model: [question,question.answers.build]) do |f|  %>
  <div class="form-control">
    <%= f.rich_text_area :body %>
    <%= f.submit "Create answer",class:"btn btn-primary mt-1" %>
  </div>
<% end %>
